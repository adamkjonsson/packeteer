# Plan — v0.12.0: a compiled protocol becomes indistinguishable from a built-in

*Written 2026-08-29. Last updated 2026-08-30, when #121 closed the milestone's work.*

*No open questions. [Q4](#q4) was the last, and #117 settled it — see
[the umbrella plan](user-defined-protocols.md#q4).*

| Issue | Status |
|-------|--------|
| #122 | **Done** — `8e09c81`, `2d98bd0` |
| #124 | **Done** — `336fd8a`, `5541e84` |
| #123 | **Done** — `0089ee3` |
| #125 | **Done** — `d2d6036` |
| #89 | **Done** — `cbea07e` |
| #126 | **Done** — `dc751bd` |
| #128 | **Done** — `76da11f` |
| #129 | **Done** — `806a7c7` |
| #130 | **Done** — `f552fb1` |
| #117 | **Done** |
| #132 | **Done** — `11db244` |
| #118 | **Done** — `8a8aa51` |
| #119 | **Done** — `ce30a0a` |
| #120 | **Done** |
| #133 | **Done** |
| #134 | **Done** |
| #84 | **Done** |
| #121 | **Done** |

**All eighteen are done**, on `v_0.12.0`. *Done* here means merged into the
branch with tests, docs and a changelog entry; the issues themselves stay open
until the release closes them with the milestone.

**Seven were planned and eleven were not.** The plan opened with #117 to #121,
#89 and #84. The other eleven — #122, #123, #124, #125, #126, #128, #129,
#130, #132, #133, #134 — arrived during the work: #89's capture collecting
produced six of them, #126 came with them, #132 turned up inside #117, and
#133 and #134 inside #120's harness on its first run.

| Issue | Summary |
|-------|---------|
| [#122](https://github.com/adamkjonsson/packeteer/issues/122) | `sanitise` leaves addresses and MACs in ICMP payloads, and the leak makes its own IPv6 substitution reversible |
| [#124](https://github.com/adamkjonsson/packeteer/issues/124) | packeteer cannot read a loopback capture: `DLT_NULL` and `DLT_LOOP` are unsupported |
| [#123](https://github.com/adamkjonsson/packeteer/issues/123) | An unsupported link type parses to an opaque payload in silence, so `sanitise` leaves the whole packet intact |
| [#125](https://github.com/adamkjonsson/packeteer/issues/125) | `sanitise` leaves the client MAC and hostname in DHCP options |
| [#126](https://github.com/adamkjonsson/packeteer/issues/126) | A truncated capture does not round-trip: `parse` → `build` silently un-truncates it |
| [#128](https://github.com/adamkjonsson/packeteer/issues/128) | A wrong ICMP checksum is silently recomputed, and a fragmented ICMP datagram does not round-trip |
| [#129](https://github.com/adamkjonsson/packeteer/issues/129) | Bytes past the end of a datagram are dropped, and sanitising hides it from the corpus sweep |
| [#130](https://github.com/adamkjonsson/packeteer/issues/130) | A compressed DNS message re-encodes larger, so it never round-trips |
| [#132](https://github.com/adamkjonsson/packeteer/issues/132) | A unit referencing a later-defined unit compiles to a module that cannot be imported |
| [#133](https://github.com/adamkjonsson/packeteer/issues/133) | A switch arm that is a unit reaches a packet spec as a dataclass, so the spec is not JSON |
| [#134](https://github.com/adamkjonsson/packeteer/issues/134) | A truncated DHCP message decodes into a half-built message instead of raising |
| [#117](https://github.com/adamkjonsson/packeteer/issues/117) | A compiled protocol's sensitive fields are not redacted, and `sanitise` still reports success |
| [#118](https://github.com/adamkjonsson/packeteer/issues/118) | The CLI cannot load a compiled protocol, so `packeteer parse` never sees one |
| [#119](https://github.com/adamkjonsson/packeteer/issues/119) | `packeteer stream` can only generate the two payloads it was compiled with |
| [#120](https://github.com/adamkjonsson/packeteer/issues/120) | Nothing holds hand-written and compiled protocols to the same contract |
| [#121](https://github.com/adamkjonsson/packeteer/issues/121) | The guides still describe DNS, DHCP and HTTP as the protocols packeteer knows |
| [#89](https://github.com/adamkjonsson/packeteer/issues/89) | CI never exercises a real capture: the test corpus is untracked |
| [#84](https://github.com/adamkjonsson/packeteer/issues/84) | `parse_http` does not de-chunk a chunked body |

Five of these — #117 to #121 — finish
[plans/user-defined-protocols.md](user-defined-protocols.md). Two are older,
#89 and #84, and both turn out to be answered by things this sequence of
milestones has already settled, which is why they belong here rather than
anywhere else. The remaining five arrived during the milestone, all of them
out of #89's capture collecting, and all five are done.

## #122 — the bug that arrived with the corpus *(done)*

Found while collecting captures for #89, and it belongs to that work as much
as to this milestone: **you cannot commit a real corpus until the sanitiser
can actually clean it.**

`sanitise` had no ICMP handling at all. A Neighbour Discovery capture came out
with its Ethernet and IPv6 headers replaced and the same addresses still
inside the payload — the ND target, a link-layer option holding the real MAC,
and the whole packet quoted inside an error.

The link-layer option is what makes it a defect rather than an omission. An
IPv6 link-local address is EUI-64 derived from the MAC, so:

```
1c:0b:8b:3c:e7:a3  → flip the U/L bit, insert ff:fe
                   → fe80::1e0b:8bff:fe3c:e7a3
```

which is exactly the address that had been replaced with `2001:db8::1`. **The
output was a file that looks sanitised, reports success, and can be reversed
by anyone holding it.** A whole-file byte scan of the original "clean" capture
found all eight of its real addresses and MACs still present; after the fix,
none.

### What it settled

**`sanitise` learns structure; the parser does not.** The payload stays opaque
bytes and the round trip is untouched — the same shape `_sanitise_dns` has.

**Replacements go through the same `_Replacer`.** A MAC in a link-layer option
becomes the *same* synthetic MAC as in the Ethernet header, and a quoted
address the same one as in the frame. Substituting something different would
have been almost as misleading as substituting nothing.

**`identifier` and `sequence` were lying.** Those two fields are the halves of
a four-byte field whose meaning depends on the message type; they are named
for what an Echo puts there. For an ICMPv4 Redirect they hold halves of a
gateway *address*, which is how the third leak hid in plain sight — nothing in
the model said an address was there. Fixed by documenting them accurately in
both header classes, both parsers and the packet-spec reference, and adding
`rest_of_header` to read or write all four bytes as one value. The field names
are unchanged, so specs written against an earlier version still build.

**A type it cannot redact warns, sharing `PersonalDataWarning`** with a new
`kind` of `"unredacted"` rather than getting a class of its own. Code that
catches that warning is asking *is this file safe to share*; a separate class
would be silently missed by everyone already asking. Silence is what let the
bug survive.

## #124, #123 and #125 — what collecting captures turned up *(done)*

Both came out of #89 rather than the umbrella plan, and both were found by
trying to capture traffic rather than by reading code.

**#124 — loopback captures could not be read at all.** `tcpdump -i lo0` on
macOS produces link type 0, which packeteer did not support, so the easiest
traffic there is was unusable. It blocked corpus collection twice in an hour:
an ICMPv4 error needed a cooperating remote host without it, and an IPv6 TCP
session needed a second machine or a tunnel broker, on a laptop with no global
IPv6. With it, both are one command against `::1`.

It was moved ahead of #89 for that reason — a tool for collecting captures is
worth more before the collecting than after.

**#123 — an unsupported link type was silent**, which is how #124 presented:
the frame became an opaque payload, no warning, and the failure surfaced later
as `missing network.src`. Worse, `sanitise` then reported success on a file
where every address was still present.

Fixed three ways, following the precedent already in the codebase
(`UnsupportedIPProtocolWarning`):

- `UnsupportedLinkTypeWarning` at parse time, once per file rather than once
  per packet, since the link type is a property of the capture.
- `sanitise` warns `kind="unredacted"` for any packet that is nothing but an
  opaque payload, because it cannot redact what the parser never saw.
- `build` says the packet was never decoded and why, rather than naming the
  key that is missing as a result.

### What #123 corrected

**#122's warning kind was being relabelled.** `_consolidate_pii_warnings`
rewrote *every* `PersonalDataWarning` with a template that assumed `kind` was
`"email"` or `"name"` — so the `"unredacted"` reports #122 added came out as
*"Possible name found in…"*. A latent bug shipped in #122 and found only
because #123 raised a second warning through the same path. Findings and
reports are now distinguished, and a report keeps the wording whoever raised
it chose while still being consolidated across packets.

**Two existing tests documented the old behaviour**, and both were updated
rather than worked around: one asserted no PII warnings for a spec that is
only a payload, which is now exactly the case that warns; the other asserted
that an unknown link type returns an empty packet, and now asserts it warns
too.

**#125 — `sanitise` kept the client MAC and hostname in DHCP options.** Found
by the check that should have been routine from the start: scanning every
sanitised capture for addresses taken from its own original. `chaddr` was
redacted and option 61 was not, so the real MAC sat two fields from its own
substitution — and the two disagreed, making the capture wrong as well as
leaky. `_DHCP_OPT_HOSTNAME` and `_DHCP_OPT_DOMAIN_NAME` were defined in
`sanitise.py` and never referenced, so the omission was unfinished work rather
than a decision.

That is four of these in one cycle, after #113's ignored spec keys, #122's
unredacted ICMP payloads and #123's silent link type. **packeteer's default
failure mode has been silence**, and every one was found by checking output
rather than by reading code. The corpus work in #89 is what makes that check
automatic.

## The constraint that shapes this milestone

**v0.11.0 shipped a compiler whose output is a second-class citizen.** A
compiled protocol parses, builds and serialises exactly like a built-in, and
then falls short in three specific places:

- `packeteer sanitise` **does not redact it**, and says nothing;
- the `packeteer` command line cannot load it at all;
- `packeteer stream` cannot generate traffic with it.

The first is a defect rather than a gap. For everything else in packeteer an
unregistered protocol means a feature you do not get; there it means data you
meant to remove is still there, reported as success. That is why #117 is
first and why its open question is worth settling before it starts.

**And packeteer now has two protocol mechanisms permanently.** DNS, DHCP and
HTTP are not expressible in the spec subset — DNS needs compression pointers,
HTTP needs delimiter framing — so the built-ins stay hand-written. Two
mechanisms with no shared test is how they drift, which is #120.

---

# Part 1 — finishing the compiler's citizenship

## #117 — sanitisation from the spec *(done)*

`sensitive: true` has been in the grammar since #105 and is printed by
`packeteer protocol show`, but nothing reads it: a compiled protocol registers
with `sanitise=None`, so #101's loop skips it.

`codegen` emits a redaction function from the annotated fields and puts it on
the `AppProtocol`. It follows a field into nested units and repeated ones, and
the existing UTF-8 PII scanner (`_scan_utf8_payload`) runs over generated
`string` fields, where a name or an email is far likelier to be than in an
opaque payload.

<a id="q4"></a>
### Q4 — what happens to fields nobody annotated

The one open question on this milestone, carried from
[the umbrella plan](user-defined-protocols.md#q4).

| Answer | Consequence |
|---|---|
| Redact nothing unannotated | Silent, and the failure mode is a leak |
| Redact everything not marked safe | Safe, and makes `sanitise` useless on the protocol you added in order to *see* it |
| **Redact what is annotated, warn when a spec annotates nothing** | *Recommended* |

The warning goes through the existing `PersonalDataWarning` machinery, once
per spec rather than per packet.

This is consistent with what #102 settled for hand-written protocols — an
`AppProtocol` with no `sanitise` callable passes through untouched, documented
as a deliberate choice by whoever registered it. The difference is that a
*compiled* protocol always has a sanitiser, so "nobody wrote one" is not the
failure mode available to it; "nobody marked anything" is, and that is what
the warning is for.

**Silence is the one outcome that must not be available.**

### How it was settled

As recommended, and the mechanism is `AppProtocol.redacts_nothing` — a new
attribute a protocol sets to say its sanitiser redacts nothing, so `sanitise`
warns instead of passing the section through. A compiled protocol sets it
when its spec annotates nothing; a hand-written one may set it too, which is
a better option than the silence a missing `sanitise` callable buys.

Two things went beyond the question as posed:

- **Every string in an app section is scanned for PII**, not only a compiled
  protocol's `string` fields. The scanner previously saw the top-level
  payload and nothing else, so a name in any decoded field went unreported.
  Doing it in the walker rather than in generated code means hand-written
  protocols get it as well, and it needs no new metadata on `AppProtocol`. A
  report is not a redaction — it says a field wants `sensitive: true`.
- **`_sanitise_app_layers` now takes the packet number**, so these warnings
  name their packets the way every other finding does. The `sanitise`
  callable's own signature is unchanged, so nothing hand-written breaks.

### What #117 turned up

**#132 — a generated module with a forward unit reference could not be
imported.** `field(default_factory=Inner)` is evaluated when the class body
runs, so a spec whose units were not written in dependency order compiled to
a module that raised `NameError`. Nested units are a documented feature, and
whether a spec worked depended on the order someone happened to type it in.

`compile_spec` already parsed its output to prove it was valid, which could
not catch this: the module parses fine and fails at import. It now imports
what it generates too — in a throwaway namespace, unregistering the protocol
afterwards, and skipping when the name is already taken so it can never
disturb a protocol it did not create.

That is the eighth defect this milestone whose failure mode was silence, and
the second where the *check* was the thing that was too weak rather than the
code.

## #118 — loading a protocol from the command line *(done)*

Three mechanisms: a repeatable global `--protocol ./sensor.py`, a
`"protocols": […]` key in the packet spec so a spec is self-describing, and
`PACKETEER_PROTOCOLS` for a shell that always wants the same ones. Paths in a
spec resolve relative to the spec file.

### The rule that matters

All three import Python from a path, in a tool people point at captures they
did not produce. That is no worse than `import` — the user names the file —
but one rule follows and it is not negotiable:

> **A path must never be taken from a capture's contents.** The packet-spec key
> is a path the *user* wrote in a spec they are editing. It must not become a
> path packeteer discovers while parsing someone else's traffic.

The docs must also say plainly that a protocol module is code and is trusted
as code — #103 already says this for hand-written protocols, and #121 carries
it to the CLI pages.

### What #118 corrected

**The flag is `--load-protocol`, not `--protocol`.** `packeteer stream
--protocol tcp` has meant the transport to simulate since 0.7, and it is in
the command's own documented examples. A global `--protocol FILE` would have
collided with it outright — argparse refuses the duplicate — and even where it
parsed, one spelling meaning "which transport" in one place and "import this
Python" in another is a trap rather than a convenience. The issue proposed the
name without noticing the existing flag.

Two things were added that the issue did not ask for, both for the same
reason it gives for being careful:

- **Loading from a spec says what it is importing**, on stderr. A `protocols`
  key is Python named in a data file; executing it quietly is the failure mode
  this milestone has met eight times.
- **A module that registers nothing warns.** Importing it and getting no
  protocol is indistinguishable, downstream, from a protocol that never
  arrived — the section simply stays an opaque payload.

## #119 — driving a stream with a compiled protocol *(done)*

`--payload <registered name>`, with `http` and `vpn` still working. The
generator side is nearly free, since `TCPSession.send()` already takes bytes.

What it actually needs is a way to say **what messages to send**, which is
`--protocol-messages sensor.json` — a file of packet-spec sections, sent in
order. **This stays out of the spec grammar.** A spec describes a protocol;
which messages to send is a property of the traffic you want, and putting it
in the grammar would make every spec carry test data.

Worth remembering #83: `--payload http` silently ignored TCP anomaly options
because the impairment passes identify packets by their labels. Whatever
labels the new path emits have to be the ones those passes expect, or
impairments will be dropped the same way.

### How #83 was avoided

By not having a new path. `TCPStreamConfig.payload_fn` already existed —
a callable `(index, direction) -> bytes` — so a protocol's messages are fed
into the **ordinary** generator rather than routed around it. The labels are
then the ordinary labels because nothing generated them differently, and the
impairment passes need to know nothing about protocols at all. Asserted
directly: a stream with `--retransmission-probability 1.0` emits
`RETRANS[0]` … `RETRANS[5]` beside its `DATA[i]`.

`UDPStreamConfig` had no such hook and now does, with the same signature —
the alignment CLAUDE.md asks for, and the reason a callable written for one
transport works for the other.

### What #119 turned up

**`--load-protocol` used `dest="protocol"`**, which `stream --protocol tcp`
also owns, so `--protocol udp` was read as a *list of module paths* and
packeteer tried to import a file called `u`. #118's rename fixed the option
string and not the dest; the flag now uses `dest="load_protocol"`. Nothing
caught it because #118's tests never combined the two flags — a stream is
the only subcommand where both appear.

**`load_module` would not reload after `unregister`.** It skipped anything
already in `sys.modules`, so a protocol that had been unregistered could not
be brought back: the second call returned nothing, which looks exactly like a
module that registers nothing. It now re-executes when the protocols a path
registered are no longer registered.

**A malformed message escaped as a traceback.** These sections are user JSON
going through an encoder compiled from a spec, and a field of the wrong type
raises `AttributeError` from inside generated code — which the CLI reported
by printing a stack trace at whoever wrote the JSON. Named explicitly in
`_ENCODE_ERRORS` rather than caught blind, since ruff is right that a bare
`except Exception` hides bugs as readily as it handles input.

## #120 — one contract, both mechanisms *(done)*

A harness that takes any registered protocol and asserts the contract, run
over the built-ins and the example specs alike. The properties are listed on
the issue; the one that matters most is the last —

> `PacketBuilder.app` → `parse_packet` → packet spec → `build` is
> byte-identical

— which is currently asserted for compiled protocols and for the built-ins
separately, in different files, with different fixtures. That is exactly the
shape of a guarantee that quietly stops holding on one side.

### It is shipped, not just tested

`packeteer.conformance.check_protocol` is a module, not a test file. A
protocol someone registers is treated exactly like a built-in, so it is worth
holding to the same contract, and the guide now points at it instead of
suggesting people write the round trip by hand. It returns every failure
rather than raising at the first, so one run says everything.

### What it found on its first run

Two defects, both in shipped code, and neither reachable from the tests that
existed:

- **#133 — a `switch` arm that is a unit reached a spec as the dataclass.**
  `to_spec` emitted `Ping(nonce=42)` where a section belonged, so
  `json.dumps` refused it and `packeteer parse` produced something no file
  could hold. `from_spec` had the mirror, so the pair round-tripped **in
  memory** and broke only through a file — which is why nothing caught it.
  `examples/protocols/rpc.yaml`, one of the two shipped examples, was
  affected.
- **#134 — a truncated DHCP message decoded into a half-built message.**
  Options that ran out mid-way were accepted, so a message cut by a snaplen
  parsed into a spec saying those options were *absent*. #126 went to some
  trouble to make a truncated capture rebuild as truncated; a decoder that
  invents a shorter message undoes that one layer up.

Both are fixed here, because the acceptance criterion is that the built-ins
and the examples pass.

### One property softened, deliberately

The issue lists `decode(encode(m)) == m`. DNS returns `example.com.` for a
name written `example.com`, which is what the wire format means — a
canonicalisation, not a loss. So the harness asserts the property that
actually distinguishes the two: encoding the decoded message again must give
the same bytes. `canonicalises` reports the normalisation as information
rather than as a failure.

## #121 — the guides *(done)*

#103 and #113 documented both routes, but the pages a reader arrives at first
still describe the world as it was before v0.10.0: `docs/guide/parsing.md`
names only `.dns`, `.dhcp` and `.http`, and `generating.md` only the three
builder methods.

The thing to state once and prominently: **packeteer has two protocol
mechanisms and will keep both.** A reader who finds only one will assume the
other is legacy.

### Where it ended up

In three places, because a reader arrives at whichever of them they arrive at
first: the guide index, the head of `docs/protocols/index.md`, and the note
at the top of `adding-a-protocol.md`. Each says the same thing — both routes
produce the same `AppProtocol`, packeteer cannot tell them apart, and #120's
conformance harness holds both to one contract — and each says *why* both are
permanent, which is that DNS, DHCP and HTTP are not expressible in the spec
subset.

### More to cover than the issue anticipated

Four things landed after it was written and are documented here too:
`--load-protocol` and the three loading routes (#118, and the flag is not
called `--protocol`, which the issue assumed), `--payload <protocol>` (#119,
already written up on `cli/stream.md` when it landed),
`packeteer.conformance` (#120), and `AppProtocol.redacts_nothing` (#117).

`sanitising.md` was the page that most wanted care. It now says outright that
a field nobody marked is not redacted and the command still reports success —
the Q4 trade-off — and points at the one case packeteer can detect. The
warning ends by saying to check the output, because every redaction defect
this milestone found (#122, #125, and the ICMP payloads before them) was found
by comparing a sanitised capture against its original, not by reading code.

---

# Part 2 — two older issues this milestone answers

## #89 — a tracked capture corpus *(done)*

`testcases/` holds thirteen captures and **none of them are in the
repository**; `.gitignore` excludes `*.pcap` and `*.pcapng`. The sweeps added
with #86 and #87 therefore skip on a clean checkout, which keeps CI honest and
means the assertion runs nowhere automatic.

**Every capture CI can see is one packeteer generated itself**, and the issue
records what that costs: across the eleven synthetic captures, 0 of 1,314 TCP
packets carry any TCP option, while the one real capture has options on all
13. #87 existed because of that, and was found by hand.

### Why it belongs in this milestone rather than another

Two things this cycle argued for it, neither of them in the original issue.

The v0.11.0 release ran `2606 passed, **2 skipped**` on a clean clone, and
those two skips are exactly this corpus — so the whole spec compiler shipped
without CI ever running a real capture through the round trip.

And the CI failure at the end of v0.11.0 was the same class of problem: a test
that read files from outside the checkout, which passed locally and nowhere
else. An untracked corpus is that, standing, by design.

### What #89 turned up

Assembling it found two more defects, both by the same check — scanning each
sanitised capture for addresses taken from its own original:

- **#125**, the DHCP client MAC and hostname, fixed.
- **#126**, a truncated capture that does not round-trip. `parse` → `build`
  loses the IP header's declared length, the record's `orig_len` and the
  file's `snaplen`, so a snaplen capture comes out *complete* — smaller,
  internally consistent packets that never existed on any wire.

**#126 blocked a corpus member.** The snaplen capture was collected and could
not be committed: sanitising it destroyed the property it was collected for.
It was recorded in the manifest's known gaps rather than left to be
rediscovered, and it is now committed as `tcp_v4_snaplen.pcapng` — the fix is
what made sanitising it non-destructive.

### The option to take

The issue lists three; **option 1 — commit a small corpus** — is the only one
that closes the gap, and it says so. The others are worth naming as rejected
rather than forgotten: a generated fixture can only contain shapes someone
thought to write, which is the property that let #87 hide; and a local
pre-release check is what happens if nobody does anything.

**`http_example.pcapng` is real traffic and goes through `packeteer sanitise`
first.** Its MAC and IP addresses want a look before they land in a public
repository. That packeteer sanitises its own corpus with its own command is a
pleasing check on the command.

Worth doing what #68, #86 and #87 all did: confirm each sweep **fails without
the fix it guards**. Done, and it does: with #86's `pad: false` removed from
`_apply_ethernet`, the round-trip sweep fails on `arp.pcapng` packet 1
immediately. A sweep nobody has seen fail is a sweep nobody knows works.

### The shape it took

`testcases/real/`, eight captures, 92 KB in total, each named individually in
`.gitignore` — so adding one is an act rather than an accident, and an
unsanitised file cannot be committed by being dropped in the directory. The
synthetic captures stay untracked, which means **everything committed is real
traffic** and the distinction needs no other marker.

`MANIFEST.md` says what each capture is *for*, and a test fails if a capture
is not named in it — a capture nothing asserts anything about is a file, not a
test. Another test asserts the reasons they were collected still hold: that
`tcp_v4.pcapng` really does carry two different option layouts, that
`dns.pcapng` really does use compression pointers, that the loopback capture
really is `DLT_NULL` with offloaded checksums. Without those, the corpus
quietly becomes decoration.

### What it does not reach — #127

Nine captures, 459 packets, twelve decoders. Everything else packeteer
supports has no real traffic at all: SCTP, VLAN, MPLS, every tunnel, IPv4
fragments, IPv6 extension headers, Linux cooked capture — and, less obviously,
a classic `.pcap` file, a nanosecond capture, and an HTTP body. The last three
are missing because every capture came from one macOS laptop, one tool and one
set of flags, which tests the collector as much as the code.

Filed as [#127](https://github.com/adamkjonsson/packeteer/issues/127) and
deliberately **not** taken into this milestone. Its tier-1 captures are cheap,
but tiers 2 and 3 need a Linux host, a tunnel endpoint, or a decision about
whether third-party captures from the Wireshark archive belong in the
repository — and none of it blocks #117 to #121. A manifest gap list that
nobody owns does not shrink, so it belongs in an issue rather than in a
heading called "Known gaps".

## #126 — the truncation that did not survive a round trip *(done)*

A capture taken with `tcpdump -s 96` parsed correctly — #92 and #94 both did
exactly what they were written to do — and then rebuilt as eleven *complete*
packets in a file whose header said nothing had ever been cut. Not corrupt:
smaller, internally consistent, correctly checksummed packets that never
existed on any wire. The defect was entirely on the way back out.

### What was missing

Four facts, none of which a packet spec had anywhere to put:

| Fact | Lives in | Now |
|---|---|---|
| IPv4 Total Length / IPv6 Payload Length as captured | the packet | `network.declared_length` |
| the transport length and checksum | the packet | `transport.length`, `transport.checksum` |
| the packet's length on the wire | the pcap record | `packet_metadata.orig_len` |
| the capture limit | the pcap file header | `metadata.snaplen` |

Each is written only when a rebuild could not derive it — the rule
`transport.length` has followed since #68 — so an ordinary capture grows no
keys at all.

`packet_metadata.truncated` from #94 was already recorded, correctly. It is a
boolean, so nothing could act on it: it says a packet was cut, never how long
it was.

### One name, not two

The issue proposed `network.total_length` and `network.payload_length`,
mirroring the two header fields. The section already uses **one** name for
that v4/v6 pair — `ttl` covers both TTL and Hop Limit — and
`PacketBuilder.ip()` builds either version from one signature, so a second
name would have to be chosen by looking at `src`. `declared_length` follows
`ttl`.

### What it corrected in #92

#92 cleared `transport.length` and `transport.checksum` on a truncated
capture, so that a snaplen would not read as corruption on every packet. Its
reasoning had two halves, and #126 removes the second:

> keeping the captured ones would say "wrong on the wire" about every packet
> of a snaplen-limited capture; **and a rebuild of a truncated packet does not
> reproduce the original either way, so there is nothing for them to
> preserve.**

Once the other three keys exist, the rebuild *does* reproduce the original —
and these two values are part of what it needs. They are now kept. The first
half of the reasoning stands, and is answered by #94's flag instead: a
recorded checksum means "wrong on the wire" only on a packet without
`packet_metadata.truncated`. That is a real cost, written down in the tests
(`TestTruncationCannotVerifyCorruption`) rather than left implicit: a consumer
that reads a recorded checksum as corruption must now read two keys.

Worth noting how the two decisions differ. #92 chose between two lossy
options and picked the quieter one. #126 removed the need to choose.

### `PcapFile.packets` holds records

`read_pcap` was flattening each record to `(data, ts_sec, ts_frac)` and
discarding `orig_len` — the one place a truncated packet's real length
survives. It now keeps the `PcapRecord`, which unpacks, indexes and measures
as that tuple, so every existing loop is unaffected. The one behaviour a
record lacks is equality with a literal tuple, which two of packeteer's own
tests were asserting; that is the whole of the compatibility cost.

### How it was verified

The same way the rest of this milestone was: by checking output, not by
reading code.

```
$ packeteer parse captures/tcp_v4_snaplen.pcapng -o t.json
$ packeteer build t.json --pcap t.pcap
$ cmp captures/tcp_v4_snaplen.pcapng t.pcap
```

Byte-identical, having been through JSON and back. The intermediate states
were checked the same way at each step, which is how two mistakes surfaced:
the parse-side hook was written *before* `update_config(cfg, pkt.ip)`, whose
`_apply_ip` assigns `config["network"]` wholesale and silently dropped the new
key; and `_clone_ip` / `_clone_ipv6` name every field they carry, so the
builder override never reached the wire until they were told about it. Both
would have looked correct in review.

## #128 — the same rule, three protocols short *(done)*

Found by dry-running #127's tier-1 fragmented capture *before* collecting it.
`ping -s 4000` would have produced a capture that failed the corpus
round-trip sweep, which is a better place to learn it than after committing
the file.

`ICMPHeader`, `ICMPv6Header` and `SCTPHeader` had no `checksum` field at all,
so `parse` recorded nothing and `build` always computed a fresh one. Two
consequences, both verified before any code was written:

```
corrupt ICMP checksum 0xdead     -> 0x192d      (silently recomputed)
corrupt SCTP checksum 0xdeadbeef -> 0x5e1ad8b3  (silently recomputed)
UDP  fragmented: 3 fragments, not rebuilding identically: none
ICMP fragmented: 3 fragments, not rebuilding identically: [1]
```

For these three, a wrong checksum is usually **offload** rather than
corruption — SCTP's CRC-32c especially, being expensive enough that stacks
hand it to the card. The corpus already carries `tcp_v6_loopback.pcapng`
precisely to exercise that for TCP.

### Why it hid

`_clear_derivable_transport_fields` returned early for anything that was not
a `TCPHeader` or `UDPHeader`, so the other three never reached the rule at
all. Every ICMP packet in `captures/` and `testcases/real/` round-trips today,
because all of it is *received* traffic with correct, unfragmented checksums —
the two failing cases needed traffic nobody had collected.

### The shape of the fix

Not three more branches. The function now clears the checksum, asks
`_derived_checksum` what a rebuild would compute, and puts the captured value
back when the two differ or the derivation is impossible. One rule, five
transports, which is what the project's "similar problems, aligned
signatures" convention asks for. UDP keeps its extra `length` step, being the
only transport with a length field of its own.

The truncation rule from #126 falls out unchanged: a truncated packet keeps
its checksum, now for ICMP and SCTP too.

### What it corrected in #127

The tier-1 fragmented capture is UDP rather than ICMP, and that is now a
choice rather than a workaround — `ping -s 4000` works from this commit on.

## #129 — the bytes nobody owned, and a sweep that could not see them *(done)*

Found while regression-checking #128 by round-tripping the **unsanitised**
originals, which nothing had ever done. 34 of `arp.pcapng`'s 86 frames rebuilt
16 bytes short.

```
captures/arp.pcapng       frame sizes: {42: 52, 58: 34}   34 of 86 differ
testcases/real/arp.pcapng frame sizes: {42: 86}            0 of 86 differ
```

### Why `pad` could not express it

`EthernetHeader.pad` is a boolean: "pad to the 60-byte minimum", or "do not
pad". These frames are **58** bytes — 42 of ARP and 16 of the sender's
padding — and neither setting produces that. A boolean can carry a rule; it
cannot carry bytes. `trailer` carries the bytes, and takes precedence over
the padding rule because it is what the capture actually held.

The same defect sits one layer down: BOOTP pads a short DHCP message with
zeros after the END option, the parser stopped at END, and the encoder
appended nothing — so a real DHCP Request rebuilt 11 bytes short. Same name,
same semantics, per the project's convention that similar problems get
aligned signatures.

### The finding that matters more than the bug

**A round-trip sweep over sanitised captures cannot catch anything `sanitise`
itself discards.** `testcases/real/` is packeteer's own output — `sanitise`
parsed and rebuilt every packet in it — so the padded frames were normalised
to 42 bytes on the way in and the committed capture passed a sweep that
proved nothing about the property it was collected for.

That is #126's shape a second time, and it has now cost twice:

- the padded ARP frames, above;
- **DNS compression**, [#130](https://github.com/adamkjonsson/packeteer/issues/130).
  `captures/dns.pcapng` has 118 compressed messages; the committed copy has
  **zero**, because `build` re-encodes names in full.
  `test_dns_uses_compression_pointers` passes on it anyway — it scans for a
  byte with its top two bits set, and ordinary data bytes are often ≥ `0xC0`.
  It has never tested what it claims.

Two corpus assertions found hollow in two days, after the `*.pcapng` glob in
#127 that silently skipped both `.pcap` files. The corpus is worth what its
sweeps assert, and the sweeps want the same scepticism as the code.

### What was done about it

`TestTheOriginalsRoundTripToo` runs the check that found this, over
`captures/` when it is there and skipping when it is not. It is deliberately
**not** CI coverage — the unsanitised originals cannot be committed, which is
the whole constraint — but it is the check the manifest tells a person to run
by hand, made automatic on the machine that has the files. Packets carrying a
`dns` section are excepted by name, with #130 cited; that exception is meant
to be deleted, not to become furniture.

`arp.pcapng` was then re-sanitised and now keeps its 34 padded frames, byte
for byte, closing the manifest's link-layer-trailer gap. Every unsanitised
capture on hand round-trips except the compressed DNS.

## #130 — the question that measurement answered *(done)*

The issue carried one open question: should `build` emit compression
pointers? It was answered by measuring rather than deciding.

### What the captures say

Modelling a canonical encoder — "point at the first occurrence of the longest
matching suffix" — against every real DNS message on hand:

```
messages examined     : 476
fully-pointer names   : 516
  a canonical encoder would pick the SAME target: 282
  it would pick a DIFFERENT target             : 234
```

Compression aims at 29 distinct offsets and only 46% of pointers target the
question name. RFC 1035 §4.1.4 permits a pointer to *any* earlier occurrence,
and senders disagree about which to choose.

**So implementing compression would not have fixed #130.** It would still get
about 45% of pointers wrong, which is no round trip at all. The question as
posed was the wrong question.

### The rule, a sixth time

Record what a rebuild cannot derive. `DNSMessage.raw` keeps the captured
bytes, exactly as `TCPOptions.raw` does for TCP options and for exactly the
same reason — re-encoding is not byte-exact. `parse` sets it only when a
re-encoding differs, so an uncompressed message grows no key.

That is now #68, #126, #128, #129 and #130 all answered by one sentence. It
is worth naming as the format's organising principle rather than a habit.

### The part that is not free

`raw` wins over the decoded fields, so `sanitise` **must** delete it when it
edits the section — a redacted name still sitting in `raw` is not redacted,
which is #122 and #125's failure mode exactly. That is asserted through the
real sanitiser rather than a stub, so it cannot pass by doing nothing.

The consequence is that a captured DNS message and a redacted one cannot both
be had. All 238 messages in `dns.pcapng` are redacted, so none keeps its
compression, and the corpus gap stays open — recorded in the manifest with
the reason, since no better capture can close it. Fidelity and redaction
genuinely conflict here, unlike #126's truncation or #129's padding.

### A third hollow assertion

`test_dns_uses_compression_pointers` scanned payloads for a byte with its top
two bits set. Ordinary data bytes are often ≥ `0xC0`, so it passed on a file
with no compression in it at all, and the manifest row claiming otherwise was
false. It now asserts what that capture really carries — CNAME, SOA, OPT and
HTTPS records, none of which packeteer generates.

Three corpus assertions found hollow in this milestone, after #127's
`*.pcapng` glob and #129's sanitised-output sweep. The pattern is the same
one the code keeps showing: **the default failure mode is silence**, and a
test that cannot fail is silence wearing a passing tick.

## #84 — the chunked body, and why the answer is now clear *(done)*

`parse_http` trims to `Content-Length` and has no branch for
`Transfer-Encoding`, so a chunked message's `body` carries its chunk sizes and
terminator rather than the payload a recipient would reconstruct.

The issue lists three options and leaves the decision open, ending:

> Worth revisiting if kober or another consumer wants packeteer to hand back
> reconstructed payloads rather than decode the framing itself — which, for a
> project whose entire purpose is decoding HTTP framing, it may well not.

**v0.11.0 answered that.** Stream-shaped protocols were put out of packeteer's
scope and into kober's, on the ground that reassembly cannot serve byte-exact
reconstruction. De-chunking is the same argument in miniature: chunk
boundaries are a **sender's choice**, not a property of the payload, so a
de-chunked body cannot be re-chunked to reproduce the capture. A 70-byte body
split `1c/c/18/4` and the same body split `40/6` are different bytes on the
wire and identical afterwards.

**Recommendation: option 1 — leave the behaviour, document it.** `body` is the
encoded body, chunked framing is not decoded, and the consumer who wants the
payload is the one that decodes framing for a living.

That makes #84 a documentation issue: the `parse_http` docstring, the `http`
section of `docs/packet-spec/format.md`, and a sentence in
`docs/guide/parsing.md`. It should say *why*, not just *what*, so the question
is answered rather than rediscovered a third time.

If that recommendation is rejected, option 2 (a separate `decoded_body`) is
the honest shape, because it is the only one where both values exist at once.
Option 3 — an opt-in `dechunk=True` — should not be taken: a caller who passes
it can no longer round-trip what they parsed, which is a foot-gun rather than
a feature.

### Taken as recommended

Option 1. The reasoning is recorded in three places — the `parse_http`
docstring, the `http` section of the packet-spec reference, and the parsing
guide — each saying *why* rather than only *what*, so the question is answered
rather than rediscovered a third time.

**The argument is asserted, not just written.** `TestWhyItIsNotDeChunked`
builds the same 70-byte payload two ways, shows the wire bytes differ, shows
they are identical once de-chunked, and shows both round-trip while the
framing is kept. A rationale in a docstring is a claim; a rationale with a
test behind it is a decision.

There is also real evidence now, which there was not when the issue was
written: `http_body.pcap` joined the corpus in #127 and carries a genuinely
chunked response whose terminating `0\r\n\r\n` arrived in its own TCP
segment. Every packet of it rebuilds byte for byte, which is the property this
decision protects, asserted against traffic nobody wrote for packeteer.

One small correction: the issue illustrates the argument with the splits
`1c/c/18/4` and `40/6`, said to be of a 70-byte body. The first sums to 68.
The tests use `1c/c/18/6`, which does sum to 70; the argument is unaffected.

---

## Order of work

0. **#122** — done. It had to come first in practice: a real corpus cannot be
   committed until `sanitise` can clean it.
1. **#89** — the corpus. First of the rest, and deliberately: it is cheap, and it makes CI
   meaningful for everything after it. Shipping the rest of this milestone
   against synthetic captures only would repeat v0.11.0's blind spot.
2. **#126** — done. Not planned for; the corpus produced it, and it had to be
   fixed before the capture that found it could join the corpus.
3. **#117** — sanitisation. The defect, and the one with an open question.
4. **#118** — CLI loading.
5. **#119** — `stream --payload`.
6. **#120** — the conformance suite. Needs #117, or two of its properties
   cannot be asserted uniformly.
7. **#84** — the chunked-body decision, written down.
8. **#121** — the guides, last, describing the finished shape.

#89 and #84 are independent of the rest and of each other.

**It went in that order**, and every step held. #121 last was worth it: four
things it had to document — `--load-protocol`, `--payload <protocol>`,
`packeteer.conformance` and `AppProtocol.redacts_nothing` — did not exist when
the issue was written, and one of them is not even called what the issue
assumed.

## Sizes

Done, measured — insertions per issue, tests and docs included, the corpus's
binary captures excluded:

| Work | Actual |
|---|---|
| #122 ICMP redaction, and the `identifier`/`sequence` renaming | 765 |
| #124 `DLT_NULL` / `DLT_LOOP` support | 550 |
| #123 the three unsupported-link-type warnings | 398 |
| #125 DHCP option redaction | 169 |
| #89 corpus, `.gitignore`, manifest, sweeps | 403 |
| #126 the four truncation keys, and the pcap writers | 658 |
| #127 tier-1 captures, and the sweep that skipped them | 108 |
| #128 one checksum rule for five transports | 352 |
| #129 `trailer`, and the originals sweep | 354 |
| #130 `dns.raw` | 334 |
| #117 + #132 the generated sanitiser | 627 |
| #118 loading a protocol from the CLI | 553 |
| #119 `--payload <protocol>` | 479 |
| #120 + #133 + #134 the conformance harness | 652 |
| #84 the chunked-body decision | 232 |
| #121 the guides | 232 |

**6,965 lines** on the branch, tests and docs included, the corpus's binary
captures excluded — against **~1,800** estimated for this milestone in the
umbrella plan, and ~2,650 once #89 and #84 were added to it.

The overrun is the eleven unplanned issues, and it is the good kind: ten of
them were defects that had already shipped, and #124 is the feature that had
to exist before the captures exposing several of them could be collected at
all. None would have been found by finishing the planned work faster — every
one came from *looking at output*.

## Cross-cutting

- **Version.** `pyproject.toml` goes to `0.12.0.dev0` at the start.

- **Semver.** Mostly additions and no removals; `--payload` accepting more
  names is not breaking. #126 carries the milestone's two `Breaking:` markers,
  both narrow: `PcapFile.packets` holds `PcapRecord` objects, which unpack and
  index as the old tuple but no longer compare equal to one; and a truncated
  packet keeps its `transport.checksum`, so a consumer reading a recorded
  checksum as corruption must now test `packet_metadata.truncated` beside it.
  A 0.x minor bump allows both, and both are called out.

- **The umbrella plan finishes here.** Once #117–#121 land,
  [plans/user-defined-protocols.md](user-defined-protocols.md) is delivered in
  full, and the standing cost it names — two app-layer mechanisms, kept
  indefinitely — becomes a permanent property of the project rather than a
  transitional one. #120 is what makes that sustainable.

- **kober.** #84's answer leans on the split v0.11.0 settled, so it is worth
  telling them: packeteer will not de-chunk, on the same ground it will not
  reassemble.

- **Release.** All issues close at release with the milestone, not at merge.
